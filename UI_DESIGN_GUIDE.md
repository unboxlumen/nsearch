# UI 设计指南（通用设计理念 v1）

> 定位：一份可跨工程复用的设计理念，**不绑定任何具体项目**。  
> 用法：新项目启动时复制本文件到工程根目录，按「平台落地参考」补全对应平台小节；设计评审时对照第 10 节清单逐项打勾。  
> 核心立场：**原型是「意图」，平台原生组件是「稳妥的实现手段」**，两者不冲突。

---

## 0. 一句话理念

用平台原生组件，做扁平克制的界面，靠留白建立层级，与原型保持信息结构一致即可——**不必像素级 1:1**。动效只用于「解释从哪来、到哪去」和「确认操作」，不做装饰。

---

## 1. 五条核心原则（按重要性）

1. **原生优先（Native-first）**  
   能用平台原生控件 / 设计系统组件就用，不为炫技自定义非标控件或特效。原生组件自带一致性、可达性与暗色支持。
2. **扁平克制（Flat & restrained）**  
   不滥用阴影、渐变、发光、霓虹。层级感来自留白与对比，而不是投影。
3. **留白即设计（Whitespace is design）**  
   屏幕外边距、组间距、项间距都是设计的一部分，不是「默认 0」。空不是没设计，是设计本身。
4. **一致大于个性（Consistency > novelty）**  
   同一控件在所有页面行为一致；遵循目标平台的规范（Material / HIG / 各桌面 OS 约定），用户无需重新学习。
5. **粗浅模仿即可（Good-enough fidelity）**  
   与原型在信息架构、布局、层级、主色上一致就够了；字体、图标、默认间距受平台约束，微调即可，强行 1:1 反而别扭。

---

## 2. 空间规则（最常被忽略，也最影响观感）

| 规则                  | 建议值（安卓）              | 说明                                                                    |
| ------------------- | -------------------- | --------------------------------------------------------------------- |
| 屏幕级外边距 outer margin | 手机 16dp / 平板 24–28dp | 内容**不要贴边**；根布局只处理系统栏 inset，内容内边距交给内部滚动容器                              |
| 列表行内容边距            | 左右各 16dp             | 行（触控区）占满全宽，内容用**条目根布局 padding** 缩进 16dp；不要用列表容器 padding（见 §6「列表行」）        |
| 项间距 item gap        | 8–16dp               | 列表、卡片每行之间留间距；**末项补底部间距**                                              |
| 扁平表达                | elevation = 0        | 分隔 / 边界用 1dp 描边，不用投影；如 MaterialCardView 的 elevation 设 0 + strokeColor |
| 对齐                  | 统一栅格                 | 同容器内元素左对齐或统一栅格，避免随意错位；编辑态控件组右边缘须与正常态文字右间距**对齐**，避免视觉跳动            |
| 基准网格 base grid     | 8dp（细节 4dp）          | 组件尺寸与间距对齐 **8dp 网格**，文字 / 图标内部细节用 4dp；与平台网格基准一致（Material / HIG 均为 8 的倍数） |

反例（改前常见问题）：标题栏贴边外边距=0、列表项 0 间距、卡片用阴影做层级。

---

## 3. 视觉重量与层级

- 层级来源优先级：**间距 > 字重 > 颜色深浅 > 大小 > 阴影**（阴影放最后，且尽量不用）。
- **单一主色**：一个主色（如 `#007DFF`）用于关键操作 / 选中态，其余用中性灰阶。
- **文字层级用不透明度表达，不靠多写灰色**：亮背景黑字按 87%（主要）/ 54%（次要）/ 38%（提示、禁用）降透明度；暗背景白字按 100% / 70% / 50%。为每级文字单独定义灰色是反模式。
- 状态区分：选中 / 禁用 / hover 用颜色或描边表达，不要靠阴影堆叠。
- 背景：浅色界面用白底 + 浅灰背景分区，避免大面积重色。

---

## 4. 动效与过渡（Motion）

动效是「反馈与空间连续性」的工具，**不是装饰**。原型图通常不含动效，所以这一节全靠实现侧自觉补齐。

- **有目的才动**：动效用在三类场景——解释「从哪来、到哪去」（页面转场）、确认操作（点击反馈）、引导注意（状态变化）。不要为动而动。
- **短而稳**：安卓标准时长 150–300ms；进入/退出 200–300ms，简单属性变化 150–200ms。过长显拖沓，过短显廉价。
- **标准缓动**：用平台标准 easing（安卓 `FastOutSlowIn` / `LinearOutSlowIn`），不要自定义弹跳 / 果冻曲线，除非产品语言明确需要。
- **用原生转场 API**：页面转场用平台导航/转场框架（安卓 `FragmentTransaction` / `MaterialMotion`、iOS 自定义转场、Web View Transitions），不要手搓帧动画。
- **尊重 reduced-motion**：系统开启「减少动态效果」时关闭或大幅削弱动画（位移/缩放→直接切换，保留淡入即可）。这是可达性要求，不是可选项。
- **克制**：同一屏同时进行的动画 ≤ 1–2 个；避免无限循环、闪烁、视差、扫光等干扰性效果。
- **性能**：动画只改可合成属性（`transform` / `opacity`），避免触发 layout/paint；列表项动画注意 ViewHolder 复用与回收，防止掉帧。

---

## 5. 可达性（原生组件白送，自定义容易丢）

- 触控目标 ≥ 48dp（手机）/ 44pt（iOS）。
- 纯文字上下文菜单（长按弹出的选项列表）可采用 **40dp 紧凑行高**，属触控目标规则的**有意例外**：牺牲少量触控面积换取紧凑观感，已用于「简单弹出菜单」（SimpleMenu）；其余可交互元素仍须 ≥ 48dp。
- 文字对比度达标（WCAG AA：正文 ≥ 4.5:1）。
- 尊重系统字体缩放、暗色模式、系统栏安全区。
- 动效前先判断系统「减少动态效果」设置（见第 4 节）。

---

## 6. 平台落地参考：安卓（参考实现）

**单位与密度**

- 逻辑单位：`dp`（布局）/ `sp`（文字，随系统字体缩放）；`1dp = 1px @160dpi`，其余密度按倍率换算。
- 密度档位：mdpi 1x / hdpi 1.5x / xhdpi 2x / xxhdpi 3x / xxxhdpi 4x。适配看**分辨率与密度档位**，不看屏幕尺寸。
- 图标一律矢量（见 §12）；位图优先 WebP（同质量体积小），按密度目录多套放置前先与开发确认实际支持的档位。

**外边距**

- `WindowInsets` 监听里只拿系统栏 inset 做安全区，**不要**用 `setPadding` 覆盖根布局内容内边距。
- 内容内边距放到内部滚动容器（`NestedScrollView` / `RecyclerView` 的 padding）。

**扁平**

- `MaterialCardView`：`app:cardElevation="0dp"` + `app:strokeColor` + `app:strokeWidth="1dp"`。

**间距**

- 统一用 `RecyclerView.ItemDecoration`（横竖布局都支持，末项补底部间距），别在布局里零散写 margin。

**列表行**

- **触控区整行满宽**：行 `match_parent` 占满宽度，点击水波纹铺满整行；内容左右各 `16dp` 用**条目根布局 padding** 实现（不要放列表容器的 padding，否则触控区贴边、行内容出现左右空隙）。
- **图标统一 `24×24dp` 正方形**、垂直居中（48dp 单行内上下各 12dp），禁止混用 20/24/28dp；`drag_handle` 用标准 Material 图标（24dp），不加内 padding、不拉成非正方形。
- **元素间距统一**：图标→文字 `16dp`；复选框 / 拖动条等操作控件与文字 `8dp` 功能分组间距；操作控件组**右边缘 = 16dp**，与正常模式文字右间距对齐，避免进入编辑态时右侧视觉跳动。
- **行高**：单行 ≥ 48dp（触控目标），双行（标题 + 元信息）≥ 56dp。
- **固定高度栏不被 inset 挤压**：状态栏 inset 一律加到内容容器（如根 `CoordinatorLayout` 内层 LinearLayout）；固定高度栏（48dp 标题栏 / Toolbar）**禁止**被 inset padding 塞入，否则子元素被压缩、文字上下裁切。

**动效**

- 转场用 `FragmentTransaction.setCustomAnimations`（配对进入/退出）或 Material Motion；点击反馈用 `RippleDrawable`（原生默认）。
- `ViewPropertyAnimator` 做简单属性变化，时长 150–300ms，easing 用平台默认。

**原生组件映射表**

| 原型元素     | 安卓原生组件                          |
| -------- | ------------------------------- |
| 标题栏      | `Toolbar` / `MaterialToolbar`   |
| 列表       | `RecyclerView` + Adapter        |
| 底部导航     | `BottomNavigationView`          |
| 侧边栏（Pad） | `NavigationView`                |
| 搜索       | `SearchView` / ActionBar 搜索     |
| 卡片       | `MaterialCardView`（elevation=0） |

**响应式**

- 用资源限定符分布局（如 `res/layout/` 手机、`res/layout-sw600dp-land/` 平板），同一 `Activity` 按布局是否含某视图切换逻辑。

**资源与命名**

- 切图命名 `种类_位置_功能_状态`（如 `btn_home_search_normal.png`）：全小写英文 + 下划线，数字不打头；同位置同状态的切图规格统一、尺寸取偶数，安卓按密度目录（`drawable-mdpi/hdpi/xhdpi/...`）放置。

---

## 7. 平台落地参考：其他平台（填空模板）

- **Web**：用原生语义标签或设计系统（Material Web / Ant Design 等）；扁平用 `border` 而非 `box-shadow`；间距用设计 token / CSS 变量统一管理；转场用 View Transitions API 或框架路由动画；尊重 `prefers-reduced-motion`。
- **iOS**：用 UIKit / SwiftUI 原生组件；用 safe area inset 处理刘海；层级用背景色差而非阴影；转场用系统 `UINavigationController` 动画或自定义 `UIViewControllerAnimatedTransitioning`；`reduce motion` 用 `UIAccessibility` 判断。
- **Flutter**：用内置 Material / Cupertino widget；`elevation: 0` + `shape` 描边；间距用 `SizedBox` / padding 常量；页面用 `Navigator` 默认转场或 `PageRouteBuilder`；`Motion` 库做共享元素。
- **桌面**：遵循各 OS 原生外观与间距规范；保证键盘 / 焦点可达。

> 把对应平台小节补全后，本文件即可作为该项目的唯一事实来源（single source of truth）。

---

## 8. 易忽略的盲点（Blind Spots）

原型图往往只画「有数据、正常态」，实现时必须自己补上这些：

- **空 / 加载 / 错误三态**：每个列表和表单都要有 empty / loading / error 状态，不能只有「有数据」一种。原型常漏画，实现必须补。
- **过长文本 / 国际化**：文案长度会变（中→英/德更长），用 ellipsize / 换行而非固定高度截断；RTL 布局方向要可镜像。
- **键盘 / 焦点导航（Web、桌面、TV）**：Tab 顺序合理、可见焦点环，不能有「只能鼠标点到」的死路。
- **暗色模式真实落地**：不是简单反色；检查图片 / 描边 / 半透明在深色的对比度，避免「能看但刺眼」。
- **性能与过度绘制**：扁平 ≠ 无成本；大列表、圆角裁剪、模糊背景都会掉帧，注意 overdraw 与回收。
- **反馈完整性**：操作要有结果反馈（Toast / Snackbar / 震动），失败要可重试，不能「点了没反应」。
- **手势冲突**：滑动删除 / 下拉刷新 / 横向滚动共存时，明确优先级，避免误触。
- **减少动态效果**：见第 4 节，动效前先判断系统设置（晕动症用户直接不可用）。

---

## 9. 反模式（明确不要）

- ✗ 屏幕外边距 = 0（内容贴边）
- ✗ 列表行内容贴边（无 16dp 内边距）；图标大小 / 间距混用（20dp 与 24dp 混排、12dp 与 16dp 间距混用）
- ✗ 把状态栏 inset padding 加到固定高度标题栏上，导致文字被上下裁切
- ✗ 用投影 / 阴影做层级
- ✗ 列表项之间 0 间距
- ✗ 为「好看」自定义非标控件 / 花哨动画 / 渐变发光
- ✗ 装饰性、长时间、弹跳类自定义动画（不尊重 reduced-motion）
- ✗ 只做「有数据」状态，没做空 / 加载 / 错误态
- ✗ 追求像素级 1:1 还原原型
- ✗ 忽略系统栏 inset，导致内容被状态栏 / 导航栏遮挡
- ✗ 动画改 layout/paint 导致掉帧

---

## 10. 发布前自检清单（UI Review Checklist）

- [ ] 屏幕四周有外边距（非贴边）
- [ ] 列表行触控区满宽、内容 16dp 内边距，图标统一 24dp 正方形且间距一致，编辑态控件右边缘与文字右间距对齐
- [ ] 卡片 elevation = 0，用描边而非阴影
- [ ] 列表 / 分组项之间有间距，末项有底部间距
- [ ] 全部使用原生 / 设计系统组件，无炫技自定义
- [ ] 主色单一，层级靠留白与对比建立
- [ ] 动效有目的、≤ 300ms、用标准 easing 与平台转场 API
- [ ] 已处理 reduced-motion（系统开启时削弱 / 关闭动画）
- [ ] 列表 / 表单具备 空 / 加载 / 错误 三态
- [ ] 长文本用 ellipsize，支持 RTL / 国际化
- [ ] 触控目标达标、文字对比度达标
- [ ] 图标遵循矢量图标规范：统一视口、线宽一致、fillColor 用主题色（见第 12 节）
- [ ] 键盘 / Tab 焦点可达，有可见焦点环
- [ ] 暗色模式真实落地（非简单反色）
- [ ] 动画只改 transform/opacity，无掉帧、无过度绘制
- [ ] 安卓与原型信息结构一致（不必像素级一致）

---

## 11. 维护约定

- 原则更新时，保持本文件为唯一事实来源，各项目同步拷贝。
- 新平台落地经验回填到第 7 节，动效 / 盲点踩坑回填到第 4 / 8 节，避免重复踩坑。
- 本指南与具体工程代码解耦：原则是通用的，实现细节随平台而变。

---

## 12. 图标规范：SVG → VectorDrawable（通用原则 + 工程落地）

> 适用：把设计稿 / 用户提供的 SVG（IconFont、iconfont 素材等）转成 App 内矢量图标时，按本节规则统一视口、线宽与主题适配，避免「每个图标一个样」。

### 12.1 统一视口（Viewport）

- 所有矢量图标**统一 `viewportWidth/Height = 1024`**，渲染尺寸统一 `24dp × 24dp`。
- 素材常见三种视口：24（Material 标准）、960（Material Symbols）、1024（图标库 / 用户提供 SVG）。转换时统一换算到 1024，禁止 24 / 960 / 1024 / 1025 混用。
- 非标准视口一律修正：如 `viewportWidth="1025"` 改为 `1024`。
- 视口换算不改变形状比例：24 视口 × 42.667、960 视口 × 1.0667 等比放大后居中到 1024 画布。

### 12.2 线宽统一（关键，踩过坑）

- **统一基准**：1024 视口下笔画厚度统一为 **85.3 单位**（等价于 Material 24 视口的 2 单位），与 Material 系图标一致。
- **实心图标按「视口等比缩放」统一**：24 视口 → `scale = 42.667`，960 视口 → `scale = 1.0667`。形状越小，墨区越小，但笔画恒为 85.3。
- **禁止按形状「撑满画布」归一化**：把每个形状放大到同一目标框（如 853）会稀释小形状，导致小图标笔画被放大到 106 甚至更粗（返回键 16 单位放大 53.3 倍 → 笔画 106.7），与主页图标（85.3）明显不一致。
- **描边图标**：24 视口下 `strokeWidth = 2`（换算后同为 85.3）；与实心图标混用时保持同一笔宽基准。
- **用户提供的原生 1024 SVG**（如主页、标签页、添加到桌面）：保留原始设计比例，不做几何重绘；仅统一颜色属性。
- 溢出画布的素材（原路径超出 viewport 会被裁切）需缩放回安全框内，如 `icon_sniff` 原 bbox 到 x=1225，缩放 0.735 修正。

### 12.3 安全边距 / 留白

- Material 24 视口图标自带 2 单位安全边距（约 8.3%），等比放大后保留，无需额外处理。
- **满幅实心图标**（形状铺满整个 1024 画布，如历史、主题类图标）需缩放适配 **853 × 853 居中安全框**（四边留 85.3 边距），否则视觉上比其他图标大一圈。
- 用户提供的「满铺型」图标（如主页房屋，y 从 51 到 939）按设计保留，不强制套安全框。
- 非正方形形状（三点菜单、箭头、链接等）保持宽高比，**居中**于画布，禁止拉伸。

### 12.4 日间 / 夜间模式自切换（硬性要求）

- 所有图标 `fillColor` 一律使用 **`?attr/colorText`**（日间 `#121212` / 夜间 `#fafafa`，随主题自动切换）。
- 转换时把 SVG 中硬编码的填充色（`fill="#333333"` / `#000000` / `#212121` 等）**全部替换**为 `?attr/colorText`，禁止保留固定色值。
- **禁止** `?android:attr/textColorSecondary`（主题未定义时深色下回退系统灰色，图标发灰）；**禁止** `android:tint` 与 fillColor 混用（部分机型渲染不一致）。
- 多色 / 状态图标（如 checkbox）同样只保留主题色，选中态用控件状态而非换色。

### 12.5 SVG → VectorDrawable 转换五步法

1. **pathData 原样保留**：直接粘贴 SVG 的 `d` 值（Android PathParser 支持 `a/A` 圆弧命令、相对 `l/h` 隐式重复参数）。
2. **视口**：保留素材原始 viewBox，`viewportWidth/Height` 用原值；`width/height` 统一 `24dp`。
3. **颜色**：所有填充色替换为 `?attr/colorText`。
4. **多个 path**：原样并列（如文件夹+加号、手机+箭头），各自 `fillColor="?attr/colorText"`。
5. **归一化**：按 12.1 / 12.2 / 12.3 统一视口、线宽与安全框；非 1024 视口用 `scale = 1024 / 原视口` 的 group 变换。

### 12.6 被多处引用的图标

- 修改图标前**全局搜索**该 drawable 的引用；若被多处引用（如标签页图标同时用于底部导航与链接菜单），**不得直接改原文件**。
- 正确做法：新建独立文件（如 `icon_tab_page.xml`），只更新目标引用处的资源 ID，其余引用保持原样。

### 12.7 验证流程（每批图标必做）

1. **脚本归一化 + 几何校验**：批量转换后打印每个图标缩放前后的 bbox，确认目标框 / 居中正确。
2. **浏览器渲染验证**：生成日间 / 夜间双区预览页（`?attr/colorText` 用对应色值替换），用 `getBoundingClientRect` 校验每个图标实际墨区尺寸与填充色，issues 归零。
3. **资源编译**：跑 `./gradlew :app:processDebugResources`，确认 vector XML 对 Android 有效。
4. **特例白名单**：描边图标、启动器图标、用户原图不套安全框，验证时单独确认。

### 12.8 反模式（明确不要）

- ✗ 视口混用（24 / 960 / 1024 / 1025 并存）
- ✗ 按形状撑满画布归一化（小图标笔画被放大、线宽不一）
- ✗ fillColor 写死色值 / 用 `textColorSecondary` / 混用 tint
- ✗ 拉伸非正方形形状破坏宽高比
- ✗ 直接改被多处引用的图标文件

### 12.9 工程落地（nbrowser 示例）

- 图标目录：`app/src/main/res/drawable/`；命名按功能前缀分组（`bookmark_*` / `download_*` / `icon_*`）。
- 批量归一化脚本：`normalize_icons_v2.py`（24 / 960 视口等比 → 1024，描边 / 启动器 / 用户原图白名单跳过，`ic_folder` 类固定色修复）。
- 满幅图标修正：`fix_full_icons.py`（历史、主题三态图标缩到 853 安全框）。
- 预览与验证：`gen_preview.py`（日 / 夜双区 HTML）+ `verify_icons.py`（Playwright 几何校验）+ `shot_preview.py`（截图）。
- 已统一结果：70 个图标线宽统一 85.3 单位，日 / 夜模式自动适配，全部通过渲染验证与资源编译。
